/** Summary from OpenRouteService Directions (and similar). */
export type RouteSummary = {
  /** Route length in meters */
  distanceM: number
  /** Travel time in seconds */
  durationSec: number
}
